import { MapScreen } from "@/src/screen";
import { Text, View } from "react-native";

export default function Map() {
  return (
    <View style={{flex:1}}>
      <MapScreen/>
    </View>
  );
}
